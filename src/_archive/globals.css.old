@import "tailwindcss";

/* ==========================================================================
   🎨 AnotherSEOGuru Agency - Complete Design System
   All colors use HSL format for theme flexibility
   ========================================================================== */

:root {
  /* Background & Surface */
  --background: 210 20% 98%;
  --foreground: 222 47% 11%;

  --card: 0 0% 100%;
  --card-foreground: 222 47% 11%;

  --popover: 0 0% 100%;
  --popover-foreground: 222 47% 11%;

  /* Brand Colors */
  --primary: 217 91% 60%;
  --primary-foreground: 0 0% 100%;
  --primary-glow: 217 91% 70%;

  --secondary: 142 76% 36%;
  --secondary-foreground: 0 0% 100%;

  --accent: 142 69% 58%;
  --accent-foreground: 0 0% 100%;

  /* Utility Colors */
  --muted: 210 20% 96%;
  --muted-foreground: 215 16% 47%;

  --success: 142 76% 36%;
  --success-foreground: 0 0% 100%;

  --warning: 38 92% 50%;
  --warning-foreground: 0 0% 100%;

  --destructive: 0 84% 60%;
  --destructive-foreground: 0 0% 100%;

  /* Borders & Inputs */
  --border: 214 32% 91%;
  --input: 214 32% 91%;
  --ring: 217 91% 60%;

  /* Border Radius */
  --radius: 1rem;

  /* Marketing / hero glow (used by ProductHero) */
  --glow: hsl(217 91% 60% / 0.22);

  /* Fixed marketing header (brand lockup + tagline + vertical padding) */
  --site-header-height: 5rem;

  /* Marketing layout (shared homepage / platform / blog) */
  --marketing-section-y: 5rem;
  --marketing-section-y-lg: 7rem;
  --marketing-container-narrow: 48rem;

  /* Gradients */
  --gradient-primary: linear-gradient(135deg, hsl(217 91% 60%) 0%, hsl(142 69% 58%) 100%);
  --gradient-secondary: linear-gradient(135deg, hsl(142 76% 36%) 0%, hsl(158 64% 52%) 100%);
  --gradient-hero: linear-gradient(180deg, hsl(210 20% 98%) 0%, hsl(217 91% 95%) 100%);
  --gradient-card: linear-gradient(135deg, hsl(0 0% 100%) 0%, hsl(210 20% 99%) 100%);
  --gradient-accent: linear-gradient(135deg, hsl(217 91% 60%) 0%, hsl(217 91% 70%) 100%);

  /* Shadows */
  --shadow-soft: 0 1px 3px hsl(222 47% 11% / 0.06), 0 1px 2px hsl(222 47% 11% / 0.04);
  --shadow-medium: 0 4px 6px hsl(222 47% 11% / 0.07), 0 2px 4px hsl(222 47% 11% / 0.05);
  --shadow-strong: 0 10px 15px hsl(222 47% 11% / 0.1), 0 4px 6px hsl(222 47% 11% / 0.05);
  --shadow-glow: 0 0 30px hsl(217 91% 60% / 0.25);
  --shadow-xl: 0 20px 25px hsl(222 47% 11% / 0.1), 0 8px 10px hsl(222 47% 11% / 0.04);

  /* Animations */
  --transition-smooth: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  --transition-bounce: all 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  --transition-fast: all 0.15s cubic-bezier(0.4, 0, 0.2, 1);
}

@media (min-width: 768px) {
  :root {
    --site-header-height: 6rem;
  }
}

/* 🌙 Dark Mode */
.dark {
  --background: 222 47% 11%;
  --foreground: 210 20% 98%;

  --card: 217 19% 15%;
  --card-foreground: 210 20% 98%;

  --popover: 217 19% 15%;
  --popover-foreground: 210 20% 98%;

  --primary: 217 91% 60%;
  --primary-foreground: 0 0% 100%;
  --primary-glow: 217 91% 70%;

  --secondary: 142 76% 36%;
  --secondary-foreground: 0 0% 100%;

  --accent: 142 69% 58%;
  --accent-foreground: 0 0% 100%;

  --muted: 217 19% 20%;
  --muted-foreground: 215 16% 65%;

  --success: 142 76% 36%;
  --success-foreground: 0 0% 100%;

  --warning: 38 92% 50%;
  --warning-foreground: 0 0% 100%;

  --destructive: 0 62% 50%;
  --destructive-foreground: 0 0% 100%;

  --border: 217 19% 25%;
  --input: 217 19% 25%;
  --ring: 217 91% 60%;

  --gradient-hero: linear-gradient(180deg, hsl(222 47% 11%) 0%, hsl(217 91% 18%) 100%);
  --gradient-card: linear-gradient(135deg, hsl(217 19% 15%) 0%, hsl(217 19% 17%) 100%);

  --shadow-soft: 0 1px 3px hsl(0 0% 0% / 0.3);
  --shadow-medium: 0 4px 6px hsl(0 0% 0% / 0.4);
  --shadow-strong: 0 10px 15px hsl(0 0% 0% / 0.5);
  --shadow-glow: 0 0 30px hsl(217 91% 60% / 0.4);
  --shadow-xl: 0 20px 25px hsl(0 0% 0% / 0.5);
}

/* Theme inline for Tailwind v4 */
@theme inline {
  --color-background: hsl(var(--background));
  --color-foreground: hsl(var(--foreground));
  --color-card: hsl(var(--card));
  --color-card-foreground: hsl(var(--card-foreground));
  --color-popover: hsl(var(--popover));
  --color-popover-foreground: hsl(var(--popover-foreground));
  --color-primary: hsl(var(--primary));
  --color-primary-foreground: hsl(var(--primary-foreground));
  --color-primary-glow: hsl(var(--primary-glow));
  --color-secondary: hsl(var(--secondary));
  --color-secondary-foreground: hsl(var(--secondary-foreground));
  --color-accent: hsl(var(--accent));
  --color-accent-foreground: hsl(var(--accent-foreground));
  --color-muted: hsl(var(--muted));
  --color-muted-foreground: hsl(var(--muted-foreground));
  --color-success: hsl(var(--success));
  --color-warning: hsl(var(--warning));
  --color-destructive: hsl(var(--destructive));
  --color-border: hsl(var(--border));
  --color-input: hsl(var(--input));
  --color-ring: hsl(var(--ring));
  --radius-lg: var(--radius);
  --radius-md: calc(var(--radius) - 2px);
  --radius-sm: calc(var(--radius) - 4px);
  --font-sans: var(--font-geist-sans), ui-sans-serif, system-ui, -apple-system, sans-serif;
  --font-mono: var(--font-geist-mono), ui-monospace, monospace;
}

/* ==========================================================================
   Base Styles
   ========================================================================== */

* {
  border-color: hsl(var(--border));
}

body {
  background: hsl(var(--background));
  color: hsl(var(--foreground));
  font-feature-settings: "tnum" 1;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* ==========================================================================
   Utility Classes
   ========================================================================== */

/* Gradient utilities */
.gradient-primary {
  background: var(--gradient-primary);
}

.gradient-secondary {
  background: var(--gradient-secondary);
}

.gradient-hero {
  background: var(--gradient-hero);
}

.gradient-card {
  background: var(--gradient-card);
}

.gradient-accent {
  background: var(--gradient-accent);
}

/* Gradient text */
.gradient-text {
  background: var(--gradient-primary);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* Shadow utilities */
.shadow-soft {
  box-shadow: var(--shadow-soft);
}

.shadow-medium {
  box-shadow: var(--shadow-medium);
}

.shadow-strong {
  box-shadow: var(--shadow-strong);
}

.shadow-glow {
  box-shadow: var(--shadow-glow);
}

/* Markdown from content/blog (react-markdown) */
.markdown-body {
  font-size: 1.0625rem;
  line-height: 1.75;
  color: hsl(var(--foreground) / 0.9);
}
.markdown-body > * + * {
  margin-top: 1.25em;
}
.markdown-body h2 {
  font-size: 1.5rem;
  font-weight: 700;
  color: hsl(var(--foreground));
  margin-top: 2.5rem;
  margin-bottom: 0.75rem;
}
.markdown-body h3 {
  font-size: 1.25rem;
  font-weight: 600;
  color: hsl(var(--foreground));
  margin-top: 1.75rem;
  margin-bottom: 0.5rem;
}
.markdown-body a {
  color: hsl(var(--primary));
  text-decoration: underline;
  text-underline-offset: 2px;
}
.markdown-body a:hover {
  opacity: 0.9;
}
.markdown-body ul,
.markdown-body ol {
  padding-left: 1.5rem;
}
.markdown-body li + li {
  margin-top: 0.5em;
}
.markdown-body strong {
  font-weight: 600;
  color: hsl(var(--foreground));
}
.markdown-body blockquote {
  border-left: 4px solid hsl(var(--primary));
  padding-left: 1.25rem;
  font-style: italic;
  color: hsl(var(--muted-foreground));
}
.markdown-body hr {
  border: 0;
  border-top: 1px solid hsl(var(--border));
  margin: 2.5rem 0;
}

.shadow-xl {
  box-shadow: var(--shadow-xl);
}

/* Transition utilities */
.transition-smooth {
  transition: var(--transition-smooth);
}

.transition-bounce {
  transition: var(--transition-bounce);
}

.transition-fast {
  transition: var(--transition-fast);
}

/* Glassmorphism */
.glass-card {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: var(--radius);
}

.dark .glass-card {
  background: rgba(0, 0, 0, 0.2);
  border-color: rgba(255, 255, 255, 0.1);
}

/* Glow hover effect */
.hover-glow {
  transition: var(--transition-smooth);
}

.hover-glow:hover {
  box-shadow: var(--shadow-glow);
  transform: translateY(-2px);
}

/* Animated background pattern */
.bg-pattern {
  background-image:
    radial-gradient(circle at 25% 25%, hsl(217 91% 60% / 0.05) 0%, transparent 50%),
    radial-gradient(circle at 75% 75%, hsl(142 69% 58% / 0.05) 0%, transparent 50%);
}

/* Floating animation */
@keyframes float {

  0%,
  100% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(-10px);
  }
}

.animate-float {
  animation: float 3s ease-in-out infinite;
}

/* Pulse glow animation */
@keyframes pulse-glow {

  0%,
  100% {
    box-shadow: 0 0 20px hsl(217 91% 60% / 0.3);
  }

  50% {
    box-shadow: 0 0 40px hsl(217 91% 60% / 0.5);
  }
}

.animate-pulse-glow {
  animation: pulse-glow 2s ease-in-out infinite;
}

/* Shimmer effect */
@keyframes shimmer {
  0% {
    background-position: -200% 0;
  }

  100% {
    background-position: 200% 0;
  }
}

.animate-shimmer {
  background: linear-gradient(90deg,
      transparent,
      hsl(217 91% 60% / 0.1),
      transparent);
  background-size: 200% 100%;
  animation: shimmer 2s linear infinite;
}

/* ==========================================================================
   Button Styles
   ========================================================================== */

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 0.75rem 1.5rem;
  font-weight: 500;
  border-radius: var(--radius);
  transition: var(--transition-smooth);
  cursor: pointer;
  border: none;
  text-decoration: none;
}

.btn-primary {
  background: hsl(var(--primary));
  color: hsl(var(--primary-foreground));
}

.btn-primary:hover {
  background: hsl(var(--primary) / 0.9);
  box-shadow: var(--shadow-glow);
  transform: translateY(-2px);
}

.btn-secondary {
  background: hsl(var(--secondary));
  color: hsl(var(--secondary-foreground));
}

.btn-secondary:hover {
  background: hsl(var(--secondary) / 0.9);
}

.btn-outline {
  background: transparent;
  color: hsl(var(--foreground));
  border: 1px solid hsl(var(--border));
}

.btn-outline:hover {
  background: hsl(var(--accent));
  color: hsl(var(--accent-foreground));
}

.btn-ghost {
  background: transparent;
  color: hsl(var(--foreground));
}

.btn-ghost:hover {
  background: hsl(var(--muted));
}

.btn-gradient {
  background: var(--gradient-primary);
  color: white;
}

.btn-gradient:hover {
  box-shadow: var(--shadow-glow);
  transform: translateY(-2px);
}

/* ==========================================================================
   Card Styles
   ========================================================================== */

.card {
  background: hsl(var(--card));
  color: hsl(var(--card-foreground));
  border-radius: var(--radius);
  border: 1px solid hsl(var(--border));
  box-shadow: var(--shadow-soft);
  transition: var(--transition-smooth);
}

.card:hover {
  box-shadow: var(--shadow-medium);
  transform: translateY(-2px);
}

.card-gradient {
  background: var(--gradient-card);
}

/* ==========================================================================
   Section Styles
   ========================================================================== */

/* Main content offset below fixed site header (replaces ad-hoc pt-32) */
.main-below-header {
  padding-top: calc(var(--site-header-height) + 0.25rem);
}

/* Homepage / landing hero: intentional space below fixed nav */
.hero-below-header {
  padding-top: calc(var(--site-header-height) + 1rem);
}

@media (min-width: 1024px) {
  .hero-below-header {
    padding-top: calc(var(--site-header-height) + 1.5rem);
  }
}

.section {
  padding: 5rem 0;
}

.section-compact {
  padding: 3rem 0;
}

/* CTA section with extra bottom padding for footer separation */
.section-cta {
  padding: 5rem 0 6rem 0;
}

.section-hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  background: var(--gradient-hero);
  position: relative;
  overflow: hidden;
}

/* Use padding-inline only so Tailwind pt-/pb- on the same node are not reset by shorthand padding. */
.container {
  width: 100%;
  max-width: 1280px;
  margin: 0 auto;
  padding-inline: 1.5rem;
}

@media (min-width: 1400px) {
  .container {
    max-width: 1400px;
  }
}

/* Footer client logo strip — global keyframes avoid styled-jsx SSR/client class hash drift */
@keyframes footer-logo-marquee {
  0% {
    transform: translateX(0);
  }
  100% {
    transform: translateX(-50%);
  }
}

.footer-logo-marquee {
  animation: footer-logo-marquee 60s linear infinite;
}

.footer-logo-marquee:hover {
  animation-play-state: paused;
}

/* ==========================================================================
   Scrollbar Styles
   ========================================================================== */

.scrollbar-hide {
  -ms-overflow-style: none;
  scrollbar-width: none;
}

.scrollbar-hide::-webkit-scrollbar {
  display: none;
}

/* Custom scrollbar */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  background: hsl(var(--muted));
}

::-webkit-scrollbar-thumb {
  background: hsl(var(--muted-foreground));
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: hsl(var(--primary));
}

/* ==========================================================================
   Focus Visible Styles (Accessibility)
   ========================================================================== */

*:focus-visible {
  outline: 2px solid hsl(var(--ring));
  outline-offset: 2px;
}

/* Screen reader only */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}

/* ==========================================================================
   Paddle-style marketing canvas (PaddleHQ/paddle-nextjs-starter-kit) + ASG tokens
   ========================================================================== */

.asg-paddle-canvas {
  position: absolute;
  inset: 0;
  z-index: 0;
  min-height: 100dvh;
  width: 100%;
  pointer-events: none;
  overflow: hidden;
}

.asg-paddle-grain-blur {
  position: absolute;
  left: 0;
  top: -220px;
  z-index: 0;
  width: 100%;
  min-height: 1400px;
  background: url("/assets/background/grain-blur.svg") no-repeat 50% 0;
  opacity: 0.5;
  filter: saturate(1.1);
}

.asg-paddle-grain {
  position: absolute;
  left: 0;
  top: 0;
  z-index: 0;
  width: 100%;
  min-height: 1400px;
  background: url("/assets/background/grain-bg.svg") repeat;
  opacity: 0.22;
  mix-blend-mode: overlay;
}

.dark .asg-paddle-grain {
  opacity: 0.12;
}

.asg-paddle-grid {
  position: absolute;
  left: 0;
  top: 0;
  z-index: 0;
  width: 100%;
  min-height: 1400px;
  background: url("/assets/background/grid-bg.svg") no-repeat 50% 0;
  opacity: 0.35;
  mix-blend-mode: multiply;
}

.dark .asg-paddle-grid {
  opacity: 0.2;
  mix-blend-mode: screen;
}

.asg-paddle-orb-primary {
  position: absolute;
  left: 50%;
  top: -680px;
  z-index: 0;
  width: 1500px;
  height: 1500px;
  margin-left: -750px;
  min-height: 0;
  border-radius: 750px;
  opacity: 0.22;
  background: radial-gradient(
    70.71% 70.71% at 50% 50%,
    hsl(217 91% 60% / 0.45) 0%,
    hsl(142 69% 58% / 0.12) 45%,
    transparent 70%
  );
  filter: blur(48px);
}

.dark .asg-paddle-orb-primary {
  opacity: 0.32;
  background: radial-gradient(
    70.71% 70.71% at 50% 50%,
    hsl(217 91% 60% / 0.35) 0%,
    hsl(142 76% 36% / 0.2) 50%,
    transparent 72%
  );
}

.asg-paddle-orb-small {
  position: absolute;
  left: 50%;
  top: 80px;
  z-index: 0;
  width: 900px;
  height: 400px;
  min-height: 0;
  margin-left: -450px;
  background: url("/assets/background/small-blur.svg") no-repeat 50% 0;
  background-size: 100% auto;
  opacity: 0.45;
  filter: hue-rotate(-8deg) saturate(1.2);
  mask-image: linear-gradient(180deg, black 0%, transparent 85%);
  -webkit-mask-image: linear-gradient(180deg, black 0%, transparent 85%);
}

.dark .asg-paddle-orb-small {
  opacity: 0.3;
  filter: hue-rotate(0deg) saturate(0.9);
}